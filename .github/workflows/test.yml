name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    # run: yarn install --frozen-lockfile // TODO uncomment
    - run: yarn install # TODO remove line
    - run: yarn test
    - run: yarn build && yarn build-extension

  # test action works running from the graph
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    # - run: yarn install --frozen-lockfile # TODO uncomment
    - run: yarn install # TODO remove
    - uses: ./
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
